extends ../assets/html/layout

block page-config
  - 
    const pageConfig = {
        title: 'お問い合わせ',
        description: 'test',
        keywords: 'keyword test'
    };
    meta.init(pageConfig);
  - const currentPage = 'contact';
    

block style
  link(rel="stylesheet", href= cssDir + "contact.css")

block script
  script(src= jsDir + "contact.js")

block content
  main.site-main 
    section.section.section-oekaki
     